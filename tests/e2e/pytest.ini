[pytest]
# E2E test configuration
# These tests verify user workflows, not code coverage

# Disable coverage for E2E tests
addopts = --no-cov -v

# Test discovery
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
